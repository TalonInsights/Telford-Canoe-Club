/**
 * PLACEHOLDER — replaced wholesale by `pnpm supabase:types` once the cloud
 * project exists (P0-26 is BLOCKED on the account-ownership decision, see
 * STATUS.md). Do not add types here by hand; write real types in lib/.
 */
export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
